{
  "name": "capacitor-msal-electron",
  "version": "0.1.0",
  "description": "A Capacitor plugin that provides OAuth 2.0 authentication using the Microsoft Authentication Library",
  "author": "911 Secure",
  "license": "MIT",
  "main": "dist/main/index.js",
  "module": "dist/renderer/index.js",
  "scripts": {
    "prebuild": "npm run clean",
    "build": "run-p build:*",
    "build:main": "tsc --project main",
    "build:renderer": "tsc --project renderer",
    "clean": "rm -rf ./dist"
  },
  "dependencies": {
    "capacitor-msal": "file:..",
    "electron-promise-ipc": "^1.2.2",
    "keytar": "^4.7.0",
    "openid-client": "^3.1.1"
  },
  "devDependencies": {
    "electron": "^6.0.0-beta.8",
    "npm-run-all": "^4.1.5",
    "typescript": "^3.2.4"
  },
  "files": [
    "dist/",
    "package.json"
  ]
}
